Just a gh-pages deploy of github.com/enoua5/revengechessII.
Repo should be private (excect apparently not, fuck github premium) and the makefile if specific to my directory setup.
build in /docs/ because gh-pages is dumb.
